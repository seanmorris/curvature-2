<div>This is a <b>template</b>. It can have HTML, interpolation <span>cv-tags</span> and everything else View templates can use.</div><div>This is a <b>template</b>. It can have HTML, interpolation <span>cv-tags</span> and everything else View templates can use.</div><div>This is a <b>template</b>. It can have HTML, interpolation <span>cv-tags</span> and everything else View templates can use.</div><div>This is a <b>template</b>. It can have HTML, interpolation <span>cv-tags</span> and everything else View templates can use.</div>